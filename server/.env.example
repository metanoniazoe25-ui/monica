# Copy this file to .env and fill in your key, or leave it unset and
# paste a key into the app's Settings panel instead (stored in the browser only).
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-5
PORT=8787
